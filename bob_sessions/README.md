# IBM Bob task evidence — pending

No event-period IBM Bob contribution has been run for Cutover yet. Pre-event Shell connectivity checks are private and do not qualify as a repair. This folder is reserved for genuine September 25–27 event-period evidence, not pre-event reference results.

The [September 2026 IBM Bob 2.0 guide](https://lablab-ibm-bob-2-hackathon-guide.s3.us.cloud-object-storage.appdomain.cloud/index.html) requires Bob IDE as a core part of the solution and a clearly named PNG of the IDE task-session consumption summary for **every relevant task** in this repository's `bob_sessions/` folder. In Bob IDE, open **Tasks**, select the task in the correct workspace, click its header, and capture the consumption summary. Export the Markdown task history too when available as additional provenance; the September guide names the PNG screenshots as the required repository deliverable.

For each actual task, retain its task ID or displayed title, event-period UTC time, source commit, screenshot, exported Markdown, Bob-written files, and independently replayed result. Review every export and image for tokens, email addresses, or other private information before committing it to this public repository. Keep failed attempts and distinguish Bob's work from the pre-event Codex prototype. See [the provenance gate](../docs/PROVENANCE.md) and [Bob task brief](../docs/BOB_TASK.md).

For a Bob Shell task, run `python -m bob_sessions.summarize_shell --transcript PATH_TO_PRIVATE_NDJSON --artifact PATH_TO_CANDIDATE_JSON --output PATH_TO_REVIEWED_METADATA_JSON`. The script records the raw transcript hash, task ID, times, tool names and outcomes, and candidate hash without copying prompt text or tool inputs/outputs. Keep the raw stream outside the public repository. Inspect the generated JSON before publishing; it supplements rather than replaces the IDE task history, consumption screenshot, candidate diff, and independent Cutover replay.
