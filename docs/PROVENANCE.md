# Build and contribution provenance

This record separates preparation from the IBM Bob 2.0 Hackathon build. The pre-event notes below are historical; the event work is linked to actual IDE task records and executed artifacts.

## Pre-event baseline — September 23, 2026

The public source beginning at commit `c90cb2580284415ec18a1265cbb3edb4f017eab8` was built with Codex before the September 25 kickoff. It includes the deterministic SQLite rehearsal engine, fixed sample contracts, reference candidates, independent ledger, browser UI, HTTP and CLI interfaces, MCP server and Bob configuration, tests, public demo, and draft presentation assets. The passing window-safe reference is prewritten and cannot be represented as Bob's output. The prepared reference-withheld sibling workspace is also pre-event setup, not a Bob contribution.

On September 24, after the user approved the free-trial Shell license and a short-lived inference key, IBM Bob Shell completed four capped **pre-event readiness checks**. A read-only task (`2387d0973a2ae145093b2409f686b3a4`) actually called `mcp__cutover__inspect_release` against Parcel through the configured project server; the full transcript is retained privately. A second read-only task (`78aa782f9c5a4357e1cc94c7f73857bb`) ran in the Cutover custom mode, called only `inspect_release`, and left the frozen workspace unchanged under its integrity verifier. The signed-in IDE listed the first three Shell tasks under All workspaces. No Bob-authored repair or qualifying event-period development has occurred. Do not count these checks as hackathon work.

The later presentation finalizer, demo shot list, local neural narration renderer, MP4 assembler, validated custom-contract CLI and browser importer, warehouse fixture, Markdown review renderer, first-screen rehearsal action, and in-tab candidate comparison are pre-event Codex preparation too. At the pre-event freeze, the narration and clip templates rejected pending Bob evidence and no final MP4 existed. The final pre-kickoff commit is recorded in the event section below; no pre-kickoff commit is attributed to Bob.

The v0.2.3 evaluator change at commit `a32866e02f3199e8fa9118de5a75bf6c45610ce5` is also pre-event Codex work. It rejects a new reader that returns old-column values while only touching the target column in a no-op expression. The reference-withheld workspace prepared from that commit is a rehearsal environment, not a Bob-authored candidate or task session.

The v0.2.4 evaluator change at commit `980d12b6ea5fe5a96d7897249a03d33c2ec4ebb4` is pre-event Codex work as well. It checks new updates and inserts on a trigger-free snapshot, preventing a plan from passing only because an old-column trigger carries the value to the target. The reference-withheld workspace from this commit remains preparation, not event-period Bob work.

The [event page](https://lablab.ai/ai-hackathons/ibm-bob-2-hackathon) describes a 48-hour build on September 25–27, advertises Bob access at kickoff, asks for Bob-assisted files and task-summary screenshots, and encourages participants to get a head start. It does not explicitly define what pre-existing code is allowed. [lablab's general AI hackathon guide](https://lablab.ai/guide/ai-hackathons) says prior non-AI scaffolding is generally allowed while the core AI-powered functionality is usually built during the event, and directs participants to check event-specific rules. This general guide is not event-specific approval for Cutover.

## Event-period work — September 25, 2026

The [September event guide](https://lablab-ibm-bob-2-hackathon-guide.s3.us.cloud-object-storage.appdomain.cloud/index.html) requires IBM Bob IDE as a core part of the working developer workflow and a PNG task-session consumption summary for every relevant task in the public `bob_sessions` folder. Synthetic project data is allowed. The pre-event source was frozen at commit `77098a41466685761782f4e51ab073ca4bd0c72c` with tag `pre-event-codex-freeze-2026-09-25`. The event IDE used the provisioned `ibm-coding-challenge-2` account in `us-east`; the personal free trial and its Shell key were not used for qualifying work.

Bob IDE task `07a20bdb56f595035652c2e6732b2c53` began at 15:17:47 UTC in a reference-withheld workspace. Its [summary PNG](../bob_sessions/cutover_task01_parcel_warehouse_repair_07a20bdb_summary.png), [IDE history](../bob_sessions/cutover_task01_parcel_warehouse_repair_07a20bdb_history.md), and [candidate evidence](../bob_sessions/warehouse-07a20bdb56f5-evidence.json) show actual work. Bob used Cutover's read-only MCP tools to inspect and diagnose the failing Parcel release. An initial bidirectional-trigger repair failed through recursive triggers; this failure remains in the history. Bob saved a one-way bridge before backfill with explicit dual writes, then adapted it to the independent Warehouse contract. The saved [Parcel](../bob_sessions/parcel-07a20bdb56f5-candidate.json) and [Warehouse](../bob_sessions/warehouse-07a20bdb56f5-candidate.json) plans each passed 116/116 fresh bounded probes. Their four migration statements generate fewer statement-boundary probes than the prewritten five-statement 124-probe reference. The prewritten reference remains Codex preparation, not Bob output.

The second Bob IDE task `9aa1e2a2ae601940eb12f45bd6c2bf59` began at 16:24:08 UTC in the public Cutover workspace and built the [PR review gate](CI.md), its [workflow](../.github/workflows/cutover-review.yml), and focused tests. Its [summary PNG](../bob_sessions/cutover_task02_ci_review_gate_9aa1e2a2_summary.png), [IDE history](../bob_sessions/cutover_task02_ci_review_gate_9aa1e2a2_history.md), and [evidence record](../bob_sessions/cutover_task02_ci_review_gate_9aa1e2a2_evidence.json) bind the work to the task. Bob ran 67/67 focused and existing engine/reporting/contract tests successfully. Codex independently checked four fresh local gate controls and ran the full 91-test `unittest` suite. After review, Codex corrected a documentation sentence and integrated the files into the public repository; it did not attribute those edits to Bob.

Codex later extended Bob's single-case GitHub workflow into independent
Warehouse and Parcel matrix checks. It copied Bob's saved Parcel candidate
byte-for-byte to `ci/parcel-candidate.json`, explicitly copied the bundled
Parcel contract's default payloads into `ci/parcel-contract.json` for the
imported-contract CLI path, and added a focused gate test.
Bob authored the underlying gate and both repair candidates; the matrix
integration and its verification are Codex work.
Codex also added the hosted one-click negative mutation of Bob's Warehouse repair and GitHub Checks annotations after the Bob IDE tasks. The mutation is a synthetic falsification control; its fresh blocked result is not another Bob-authored candidate or an independent field incident.
Codex then added a browser scenario builder for a simple two-column SQLite rename. It generates a deterministic unsafe control and compatibility bridge, validates the generated contract, and runs both through the same engine. This starter and its UI are Codex-authored event-period enhancements, not Bob work; the generated pass is scoped to the entered synthetic contract.
Codex also replaced its own hardcoded two-case workflow matrix with a checked-in case manifest and validating discovery job. Bob's original per-case review helper remains the executor; case discovery and workflow wiring are Codex enhancements. The new workflow requires a real controlled PR run before its behavior is claimed as verified on GitHub.
The controlled [manifest PR #6](https://github.com/josepha-mayo/cutover/pull/6) passed discovery and both independent review jobs; the downloaded Warehouse and Parcel reports each independently replayed at 116/116. Codex subsequently added a browser CI handoff kit that reruns a passing custom candidate before packaging its checked-in inputs, manifest entry, and report. That packaging is Codex work; it does not change Bob's saved candidate or underlying review helper.
The [browser-to-CI control PR #7](https://github.com/josepha-mayo/cutover/pull/7) added only a generated custom contract and plan plus one manifest entry. Its first run created three passing review jobs but failed general CI because Codex's new discovery test still required exactly two cases; the failed run remains public. After that test was corrected, the [final run](https://github.com/josepha-mayo/cutover/actions/runs/36192747474) passed discovery, three independent reviews and Ubuntu/Windows CI. Warehouse and Parcel each independently replayed at 116/116; the generated custom case replayed at 124/124. The three downloaded artifact payloads are mirrored under [ci_browser_control](../evidence/ci_browser_control/README.md). This was a synthetic control and not a third Bob task.

Codex then made the guided form accept the exact synthetic incoming write and extended its downloaded kit with a comparable blocked baseline, independently replayed report and standalone witness when one is pinned. The [red/green kit](../evidence/ci_kit_red_green/README.md) was downloaded through the browser and exercised locally with Bob's review helper: `verified_block` at 44/100, `verified_pass` at 124/124, and a reproduced `R-07`/`A-01` mismatch. The form, packaging and control inputs are Codex work after the two Bob IDE tasks.
Codex also built the dry-run-first `ci.install_kit` handoff to replay and validate a downloaded kit before writing only its passing case to the CI manifest. This installer is a Codex extension; Bob's original per-case gate remains the verifier it invokes after installation.

Three real GitHub pull-request runs exercised the gate after the Bob task: [unsafe blocked 108/124](https://github.com/josepha-mayo/cutover/actions/runs/36165105646), [safe reference passed 124/124](https://github.com/josepha-mayo/cutover/actions/runs/36165215088), and [regressed blocked 100/116](https://github.com/josepha-mayo/cutover/actions/runs/36165290677). Their exact PR-head candidates, downloaded JSON/Markdown/ZIP/verdict/summary artifacts, first witnesses, and independent replay were verified. The controlled PRs were closed after retaining the runs. Because GitHub may require sign-in to view job logs, a [public evidence mirror](../evidence/ci_controls/README.md) exposes the synthetic artifacts and SHA-256 digests. The control plans were written before kickoff and are test fixtures, not Bob-authored candidates.

The Codex-built evaluator, browser, media templates, hosted demo, and independent verification are supporting scaffolding. The event contribution is Bob's saved repair and Bob's CI review gate, with failures and scope limits retained. These SQLite schedules do not model concurrent transactions, locking, crashes, PostgreSQL/MySQL, or production deployment approval.

On September 26, Codex added optional binding to an actual checked-in SQL source file. [Control PR #10](../evidence/ci_sql_source_control/README.md) changed only that file between its final two heads: Warehouse changed from 116/116 pass to 72/108 block, while its adapter JSON was byte-identical and Parcel stayed 116/116. All four final review packets passed independent replay. The original failed general check and its fixture-copy correction are retained. This extension and control are not additional Bob IDE work.

Codex then packaged the gate as a reusable GitHub Action. [Control PR #11](../evidence/ci_portable_action_control/README.md) called the pinned public Action from separately prepared consumer inputs on Ubuntu and Windows: 116/116 passes, 72/108 verified blocks, and unverified missing-input errors were all observed with retained artifacts. The four executed review ZIPs passed independent replay. The control lives in this repository and does not establish third-party adoption. The Action wrapper, packaging, and verification are Codex additions, not a new Bob task.

Codex connected that pinned Action to the browser's kit export. The exact four consumer files from a generated Dispatch handover kit passed 124/124 in [control PR #12](../evidence/ci_browser_action_control/README.md). Removing only the old-write bridge from its SQL blocked 80/116, exposing `DOCK-07` expected and `A-01` observed. Both run packets independently replayed against their exact checkouts; a live Render download matched the same four files. The kit, generated workflow, validation and controlled PR are Codex work, not another Bob task or third-party adoption.

Codex then added browser import of saved review ZIPs through the existing independent bundle auditor. It restores verified SQL, the failure trace, and an optional before/after comparison so a developer can inspect a CI failure and prepare a new local Bob repair task. The importer was checked with PR #12's actual passing and blocked packets, a paired packet, altered review text, a substituted witness script, and size limits. No uploaded witness script is executed. This extension does not authenticate archive authorship or create a new Bob task; editing restored SQL clears its verified state.

Codex added an optional canonical contract-hash lock to the gate and reusable Action, and made new browser kits include it. [Control PR #13](../evidence/ci_contract_lock_control/README.md) held unsafe SQL and adapters fixed: the original record IDs blocked 100/132, while changing only their IDs passed 132/132 without a lock. The original lock rejected those changed tests as unverified before SQL execution. The bundled bridge reference passed 124/124 on the original locked contract. All three executed GitHub packets were independently replayed against the actual run checkout. This guard, packaging and prospective synthetic control are Codex work, not an additional Bob task or customer adoption.

Codex added browser recovery for unavailable sample projects and bounded requests through response-body completion. A controlled local HTTP 503 left scenario creation available; retrying the catalog preserved the custom SQL and its 124/124 result. A separate failed rehearsal cleared that result and disabled its exports; an explicit rerun recovered. Native HTTP tests cover stalled headers, JSON and binary bodies, non-JSON proxy errors, and no automatic retries. These are demo reliability checks, not a new Bob contribution or an uptime claim.

Codex subsequently located observed migration-window witnesses in their checked-in SQL files. [Control PR #14](../evidence/ci_witness_source_control/README.md) retained a verified late-bridge block at line 8 after statement 2, a completed-rollout block without an invented window location, and Bob's prior Warehouse pass. All three downloaded packets were independently replayed and bound to the actual merge checkout; the GitHub annotation was inspected beside the SQL. The first successful run exposed an overly long single-line presentation; a later verified run split it into readable lines. New kits pin that Action version, while previous exact templates remain supported. This source presentation and control are Codex extensions, not a new Bob task or evidence of production safety.

Codex corrected the browser replay export to follow the selected failed probe instead of always exporting the shortest failure. A bounded worker reruns the suite, requires the selected observation and evidence identities to match, then executes the retained path again to obtain complete passing observations before rendering the standalone script. Local browser downloads reproduced a selected empty-string insert and a different Unicode update; changing selection during export refused the stale download. Existing review-packet formats and the evaluator remain unchanged, and this does not add a Bob task.
