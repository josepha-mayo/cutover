# Actual Bob repair task

The purpose of this session is to obtain a genuine IBM Bob contribution. These are instructions for the future session, not a transcript or completed result.

From a clean committed source project, run `python prepare_bob_session.py --python PATH_TO_MCP_PYTHON`. Open the printed reference-withheld sibling workspace in IBM Bob, confirm **Use MCP Servers** is enabled and the `cutover` project server is listed in [Bob's MCP settings](https://bob.ibm.com/docs/ide/configuration/mcp/mcp-in-bob), and choose the Cutover release engineer mode. The fixed evaluator is copied byte-for-byte, while prewritten passing plans and presentation notes are omitted. Begin a new task:

At kickoff, add `--label event-20260925` to create a fresh workspace even if a pre-event readiness workspace used the same source commit. A label only adds a safe suffix; an existing workspace is never reused or overwritten. Use a different label for another independent event task.

Before prompting Bob, run `python verify_bob_session.py --workspace PATH_TO_SIBLING` from the full source project and retain its passing JSON output. Then run `PATH_TO_MCP_PYTHON verify_bob_mcp.py --workspace PATH_TO_SIBLING` to launch the exact configured project server with the official SDK, discover the tools, and check its late-bridge witness. Neither local check proves Bob used MCP. Run the integrity verifier again after Bob writes its candidate; it checks the copied source files against the original Git commit and catches modifications to the evaluator, contract, failing examples, or MCP configuration. Re-freeze from the latest clean committed source at kickoff for the actual event task; pre-event sibling workspaces are readiness checks, not Bob task evidence.

If the IDE cannot be controlled reliably, IBM's [Bob Shell v2](https://bob.ibm.com/blog/august-2026-release/) says it shares task history with the IDE: start the event task in Shell, then reopen that same task in the IDE for the required screenshots and task-history export. A local Bob Shell 2.0.4 client was installed from IBM's SHA-256-verified package in this task's private `../../work/bob-shell/` directory, outside the public repo. The user approved its separate license and a short-lived inference key for the free trial. On September 24, Shell completed capped pre-event access checks, including a read-only `mcp__cutover__inspect_release` call from Bob itself (task `2387d0973a2ae145093b2409f686b3a4`). A second read-only check in the actual Cutover custom mode also invoked only `inspect_release` (task `78aa782f9c5a4357e1cc94c7f73857bb`); the frozen workspace passed its integrity audit afterward. The protected key and complete transcripts remain outside the public repo. The signed-in IDE listed the first three Shell tasks under All workspaces, including the MCP check; task-summary export remains to be tested during the event. These checks are **not event work or a Bob-authored repair**. The [May 2026 IBM Bob hackathon guide](https://watsonx-hackathons-2026.s3.us.cloud-object-storage.appdomain.cloud/Lablab-IBM-Bob-hackathon-guide-May-2026.pdf) required IDE use even when Shell was optional; it is a precedent, **not** the September rules. Do not rely on Shell alone for this event until the new brief explicitly permits it.

For a Shell task, run from the reference-withheld workspace with `--mode cutover-release-engineer`, `--workspace` pointing to that workspace, `--format stream-json`, `--disable-subagents`, and explicit cost/turn limits. The private `../../work/run-bob-shell.ps1` runner decrypts the key only for the Bob process and writes an unabridged NDJSON transcript under private `work/` without overwriting prior sessions. Extract task IDs, tool calls, and candidate attempts for the evidence manifest. [IBM's headless-mode guide](https://bob.ibm.com/docs/shell/getting-started/start-bobshell-non-interactive) says `bob run` preapproves its tools; the project mode therefore allows only reading, Cutover MCP calls, and edits to `work/*.json` or `work/*.md`. Audit the workspace with `verify_bob_session.py` immediately afterward. Do not claim the Shell-to-IDE screenshot path works until the real task appears in the IDE.

> Rehearse the Parcel release using inspect_release and diagnose_reference with reference=late_bridge. The late bridge passes all 76 completed-rollout probes yet fails migration-window probes. Diagnose the shortest failing replay and propose your own migration and new SQL adapter that preserve the old application contract, inserted records and rollback reads, including correct old reads and writes between migration statements. The new reader must read the target column without relying on the old column; new updates and inserts must put their values in the target on a trigger-free snapshot; and that column must retain every acknowledged value. An explicit dual-write is valid. Merely touching the target in a no-op expression, or inserting only into the old column, is not a repair. Use rehearse_candidate to test each proposal. Do not change the fixed old contract, seed records, evaluator, schedule generator, or test assertions. Save your own proposed plan to work/bob-candidate.json and explain observed failures and repair in work/bob-repair.md. Report exact final coverage and untested boundaries. Do not use the bundled window-safe bridge as proof of your own repair work.

## Evidence to retain

- Actual Bob version and displayed task/session identifier, if available.
- Original task prompt and timestamp.
- Screenshots of Bob's executed tool calls and its actual task summary.
- The IDE-exported Markdown task history and consumption-summary screenshot for each relevant task, placed in `bob_sessions/` after redacting credentials. The previous IBM event required both; verify the September brief before deciding which exports are mandatory.
- Every candidate it submits, including failures.
- The generated `SESSION_MANIFEST.json` with source commit and evaluator hash.
- Passing before-and-after `verify_bob_session.py` reports; retain any failing audit rather than masking it.
- JSON reports from rerunning its final candidate in the full source project's CLI and browser.
- Which files Bob wrote or changed, with an honest separation from pre-event preparation.

## Priority extension: a real PR review gate

The pre-event Codex baseline already includes CLI and browser import of bounded user-supplied SQLite contracts, the warehouse fixture, and JSON/Markdown review exports. These are **not Bob-authored**. After the genuine repair task, open the full Cutover project in Bob Agent mode and ask Bob to build an end-to-end GitHub Actions integration that makes Cutover useful in a release review:

> Add a small, reusable CI workflow for a repository's checked-in contract and candidate plan. Run the existing Cutover CLI, upload its JSON evidence and Markdown review even when the candidate is blocked, and fail the check when the evaluator blocks. Show the exact failing witness in the job summary with links to artifacts. Preserve the existing engine, old contract, negative controls and CLI exit semantics; do not invent a second evaluator, hide failures, or call a prewritten safe plan a Bob repair. Keep private production SQL out of the public test. Document the minimal copy/paste setup for a developer.

Acceptance: exercise the workflow in a controlled GitHub repository or CI job with the warehouse late bridge and see a red check plus downloadable blocked evidence; switch to its safe plan and see a green check with 124/124; delete one synchronization trigger and see red again. Check that evidence artifacts remain available for failures and that the job summary's hash matches the exported report. Run local and Windows/Ubuntu checks. Preserve Bob's actual task summary, changed files, failed attempts, and screenshots. If this integration cannot be verified before the deadline, retain the failure and present only what Bob actually accomplished.

## Recording sequence

Show a real failing browser run, switch to the real Bob session for the diagnosis/repair, return to Cutover, import the candidate Bob actually saved, rerun, open a successful rollback replay, then show the exact limitations. Preserve unsuccessful Bob attempts as part of the evidence.
