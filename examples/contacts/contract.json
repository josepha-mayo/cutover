{
  "project": "Relay / contact service",
  "summary": "Rename email to contact_email while old workers remain alive.",
  "old_column": "email",
  "new_column": "contact_email",
  "table": "contacts",
  "schema": "CREATE TABLE contacts (id INTEGER PRIMARY KEY, email TEXT NOT NULL);",
  "seed_sql": "INSERT INTO contacts (id, email) VALUES (?, ?)",
  "seed": [
    [
      101,
      "ada@example.test"
    ],
    [
      102,
      "lin@example.test"
    ]
  ],
  "old": {
    "read": "SELECT id, email AS value FROM contacts ORDER BY id",
    "write": "UPDATE contacts SET email = :value WHERE id = :id",
    "insert": "INSERT INTO contacts (id, email) VALUES (:id, :value)"
  }
}
