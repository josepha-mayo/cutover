services:
  - type: web
    name: cutover-rehearsal
    runtime: python
    plan: free
    buildCommand: python -m compileall -q cutover
    startCommand: python server.py --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
